# Asset Cleanup Candidates

Audit date: 2026-07-16. No asset was deleted in Phase 2.

| Candidate path | Files | Total size | Active references | Confidence | Recommended action |
|---|---:|---:|---|---|---|
| `public/animations/healthcare-crystal-loop/frames/frame-001.png` through `frame-300.png` | 300 | 214,672,447 bytes (204.73 MiB / 214.67 MB) | No runtime reference. Source input for `scripts/optimize-crystal-web-frames.mjs`; generated by `scripts/generate-crystal-loop.mjs`. | High for runtime redundancy; medium for source-value deletion | Retain through preview approval, then archive source masters outside the deployable app or delete in a separately approved cleanup change. |

## Active sequence verification

- `app/components/LabExperience.jsx` builds `/animations/healthcare-crystal-loop/webp/frame-NNN.webp` URLs and preloads/draws those files. It does not reference the PNG sequence or either manifest.
- Both PNG and WebP directories contain a complete, consecutively named `frame-001` through `frame-300` sequence. The WebP sequence totals 21,785,144 bytes.
- The matching frame numbers and optimizer script establish equivalent temporal coverage: each PNG source frame has a corresponding WebP output frame. This is structural equivalence; final visual approval should still compare a representative sample or full animation in Preview.
- No runtime, responsive, preload, or fallback path to the PNG directory was found. Removing it would not change current runtime lookup, but would remove the checked-in source inputs used to regenerate the WebP sequence.
- `manifest.json` and `webp-manifest.json` are not read at runtime. They are excluded from this deletion proposal pending a Phase 3 provenance decision.

