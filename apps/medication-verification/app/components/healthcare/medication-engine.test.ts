import {describe,expect,it} from "vitest";
import {normaliseName,normaliseRoute,normaliseTiming} from "./medication-engine";
import {caseCounts,demoCases} from "./medication-cases";
describe("normalisation",()=>{it("routes",()=>{expect(normaliseRoute("PO")).toBe("ORAL");expect(normaliseRoute("SC")).toBe("SUBCUTANEOUS");expect(normaliseRoute("PR")).toBe("RECTAL")});it("timing",()=>expect(normaliseTiming({frequencyType:"DUE_IN",intervalMinutes:60})).toBe(60));it("names",()=>expect(normaliseName("Acetaminophen")).toBe("paracetamol"))});
describe("deterministic cases",()=>{for(const [id,field] of [["CASE-X01","Dose"],["CASE-X02","Route"],["CASE-X06","Timing / frequency"],["CASE-X08","IVF volume"],["CASE-X09","IVF duration"],["CASE-X10","Status"]])it(`${id} detects ${field}`,()=>expect(demoCases.find(x=>x.id===id)?.comparison.fields.find(x=>x.field===field)?.result).toBe("MISMATCH"));it("missing fields review",()=>expect(demoCases.find(x=>x.id==="CASE-P01")?.comparison.overallResult).toBe("REVIEW"));it("version mismatch review",()=>expect(demoCases.find(x=>x.id==="CASE-P10")?.comparison.overallResult).toBe("REVIEW"));it("mismatch wins aggregation",()=>expect(demoCases.find(x=>x.id==="CASE-X01")?.comparison.overallResult).toBe("MISMATCH"))});
it("has exactly ten of each result",()=>{expect(demoCases).toHaveLength(30);expect(caseCounts).toEqual({MATCH:10,REVIEW:10,MISMATCH:10})});
